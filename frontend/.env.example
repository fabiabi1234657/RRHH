# URL del backend (solo necesaria si el frontend NO está detrás de nginx proxy)
# En Docker con nginx, dejar vacío o eliminar — nginx hace el proxy de /api
# En desarrollo local, el proxy de Vite maneja /api automáticamente
# VITE_API_URL=http://localhost:5000/api
